query GetPartyInvites {
  selfLookup {
    id
    partyInvites {
      id
      name
      description
      partyManager {
        id
        firstName
        lastName
        profilePictureUrl
      }
      users {
        id
        firstName
        lastName
        profilePictureUrl
      }
    }
  }
}
