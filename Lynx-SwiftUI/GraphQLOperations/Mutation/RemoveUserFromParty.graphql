mutation RemoveUserFromParty($partyId: ID!, $userId: ID!) {
  removeUserFromParty(partyId: $partyId, userId: $userId) {
    id
    name
    users {
      id
      firstName
      lastName
      profilePictureUrl
    }
  }
}
