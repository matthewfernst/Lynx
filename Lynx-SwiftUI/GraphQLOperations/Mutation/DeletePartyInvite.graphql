mutation DeletePartyInvite($partyId: ID!, $userId: ID!) {
  deletePartyInvite(partyId: $partyId, userId: $userId) {
    id
    name
    invitedUsers {
      id
      firstName
      lastName
      profilePictureUrl
    }
  }
}
