mutation LoginOrCreateUser($type: LoginType!, $id: ID!, $token: ID!, $email: String, $userData: [UserDataPair!]) {
  createUserOrSignIn(
    type: $type,
    id: $id,
    token: $token,
    email: $email,
    userData: $userData) {
      token
      expiryDate
      validatedInvite
    }
}
